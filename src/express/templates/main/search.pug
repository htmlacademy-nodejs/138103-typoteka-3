extends ../layout.pug

block content
  main
    section.search-page
      div(class=`search-page__wrapper ${results.length ? '' : 'search-page__wrapper--empty'}`)
        h1 Поиск
        .search.search-page__form
          form(method='get' action="/search" autocomplete='off')
            label
              input(type='search' placeholder='Что ищем?' name='search' aria-label='Поиск' value=search)
            button.search__button.button.button--transparent(type='submit') Найти
        if results
          if results.length
            each result of results
              .search__result
                time.search__date(datetime=result.createdDate) #{result.createdDate}
                a.search__link #{result.title}
          else 
            p.search-page__empty Ничего не нашлось